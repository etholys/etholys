# Copie para deploy-forge.config.ps1 (na mesma pasta) e ajuste se precisar.
# deploy-forge.config.ps1 não é versionado (está no .gitignore).

$DeployForge = @{
  SshHost      = '84.247.187.155'
  SshUser      = 'root'
  RemoteRepo   = '/opt/etholys'
  SshKeyPath   = ''
  GitBranch    = 'main'
}
