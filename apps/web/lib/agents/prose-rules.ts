/** Shared rule: the person never sees Markdown punctuation. The UI typesets. */
export const INSTITUTIONAL_PROSE_RULE = `- FORMA: o utilizador nunca deve ver #, ##, **, *, \` ou ---. Escreve prosa institucional (títulos em linha própria, parágrafos, listas 1. 2. 3.). O editor aplica a diagramação. Sem cercas de código.`;
