# Dependencies & build
node_modules/
.next/
out/

# Ambiente — nunca commitar segredos
.env
.env.local
.env.*.local

# Logs / OS
npm-debug.log*
.DS_Store

# Exportações locais de dados (podem conter PII)
exports/

# PDFs del libro FORGE (subidos en local)
public/uploads/forge-libros/*.pdf
!public/uploads/forge-libros/.gitkeep

# Playwright E2E seed state
e2e/.seed-state.json
test-results/
playwright-report/
